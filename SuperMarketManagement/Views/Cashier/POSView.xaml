<UserControl x:Class="SuperMarketManagement.Views.Cashier.POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             FontFamily="Segoe UI"
             d:DesignHeight="650" d:DesignWidth="980">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Product List -->
        <Border Grid.Column="0" Background="White" Padding="16" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <DockPanel Grid.Row="0">
                    <TextBlock Text="Products" FontSize="20" FontWeight="SemiBold" Foreground="#1E293B" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchTextBox" Width="200" Height="34" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Product DataGrid placeholder -->
                <DataGrid Grid.Row="2" x:Name="ProductsGrid" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" RowHeaderWidth="0" BorderThickness="0">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F0FF"/>
                            <Setter Property="Foreground" Value="#4C1D95"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Name" Width="*"/>
                        <DataGridTextColumn Header="Category" Width="100"/>
                        <DataGridTextColumn Header="Price" Width="80"/>
                        <DataGridTextColumn Header="Stock" Width="80"/>
                        <DataGridTemplateColumn Header="Action" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Add" Background="#623ED0" Foreground="White" Width="60" Height="25"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Right: Current Bill -->
        <Border Grid.Column="1" Background="White" Padding="16" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" Margin="8,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Current Bill" FontSize="20" FontWeight="SemiBold" Foreground="#1E293B"/>

                <!-- Receipt DataGrid placeholder -->
                <DataGrid Grid.Row="2" x:Name="ReceiptGrid" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" RowHeaderWidth="0" BorderThickness="0">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F0FF"/>
                            <Setter Property="Foreground" Value="#4C1D95"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sr No" Width="50"/>
                        <DataGridTextColumn Header="Item Name" Width="*"/>
                        <DataGridTextColumn Header="Qty" Width="50"/>
                        <DataGridTextColumn Header="Total" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="3" Margin="0,16,0,0">
                    <TextBlock Text="Grand Total: $0.00" FontSize="24" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Right" Margin="0,0,0,16"/>
                    <Button Content="Generate Bill" Height="40" Background="#4C1D95" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
