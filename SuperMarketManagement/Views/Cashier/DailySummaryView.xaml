<UserControl x:Class="SuperMarketManagement.Views.Cashier.DailySummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <StaticResource x:Key="PrimaryBrush" ResourceKey="BrushAccent"/>
        <StaticResource x:Key="PrimaryLightBrush" ResourceKey="BrushButtonHover"/>
        <StaticResource x:Key="TextPrimaryBrush" ResourceKey="BrushPrimaryText"/>
        <StaticResource x:Key="TextSecondaryBrush" ResourceKey="BrushSecondaryText"/>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource BrushPrimaryText}"/>
            <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{StaticResource BrushButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{StaticResource BrushSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrushSecondary}" Padding="16" CornerRadius="14" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Daily Sales Summary" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding SummaryTitle}" Margin="0,4,0,0" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="155" Height="36" Margin="0,0,10,0"/>
                    <Button Content="Today" Command="{Binding TodayCommand}" Style="{StaticResource ActionButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource BrushSecondary}" CornerRadius="12" Padding="14" Margin="0,0,8,0" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Total Sales" Foreground="{StaticResource BrushPrimaryText}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalSalesAmount, StringFormat=PKR {0:N2}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource BrushSecondary}" CornerRadius="12" Padding="14" Margin="8,0" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Receipts" Foreground="{StaticResource BrushPrimaryText}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ReceiptCount}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource BrushSecondary}" CornerRadius="12" Padding="14" Margin="8,0" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Items Sold" Foreground="{StaticResource BrushPrimaryText}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalItemsSold, StringFormat={}{0:N0}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="{StaticResource BrushSecondary}" CornerRadius="12" Padding="14" Margin="8,0,0,0" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Avg Receipt" Foreground="{StaticResource BrushPrimaryText}" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding AverageReceiptAmount, StringFormat=PKR {0:N2}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource BrushSecondary}" CornerRadius="14" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Receipts List" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding HighestReceiptAmount, StringFormat=Highest Receipt: PKR {0:N2}}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" DockPanel.Dock="Right"/>
                </DockPanel>

                <Grid Grid.Row="1">
                    <Border Background="{StaticResource BrushPrimaryDark}" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1" CornerRadius="10" Visibility="{Binding NoSalesMessageVisibility}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16">
                            <TextBlock Text="No sales for this date" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Try another date from the picker above." Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <DataGrid Visibility="{Binding ReceiptsVisibility}"
                              ItemsSource="{Binding Receipts}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              RowHeaderWidth="0"
                              BorderThickness="0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushPrimaryText}"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="Padding" Value="10,0"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,0"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushPrimaryText}"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="40"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BrushButtonHover}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Width="55" Binding="{Binding SrNo}"/>
                            <DataGridTextColumn Header="Receipt No" Width="120" Binding="{Binding ReceiptNo}"/>
                            <DataGridTextColumn Header="Time" Width="120" Binding="{Binding Time}"/>
                            <DataGridTextColumn Header="Items" Width="90" Binding="{Binding Items}"/>
                            <DataGridTextColumn Header="Total (PKR)" Width="*" Binding="{Binding TotalAmount, StringFormat={}{0:N2}}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
